# Required for `pnpm run release` (Zenodo upload via release-it). Replace with
# a real GitHub personal-access token (with `repo` scope) before publishing
# a release. DO NOT commit a real token: the .env file is gitignored.
GITHUB_TOKEN=
